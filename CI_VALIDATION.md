# V12 CI validation

Release validation record for the V12 repository.

- Python: 3.13.14
- Test command: `python -m pytest -q`
- Result: **113 passed**
- GitHub Actions: `pytest` and legacy `V11 tests` workflows passed for the validation PR.
- Validation PR: #9

The marker is retained as a reproducible release record; it is not required by the application runtime.
